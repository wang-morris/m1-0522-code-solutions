/* exported getFirstChar */
// define a function named getFirstChar that
//   takes one parameter, string -complete
// returns string at the first index -complete

function getFirstChar(string) {
  return string[0];
}
// define a function named getFirstChar that
//   takes one parameter, string
// returns string at the first index
