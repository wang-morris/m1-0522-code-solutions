/* exported isLowerCased */
// a function is being defined named isLowerCased
//   that takes in one parameter, word -completed
//  set a variable result equal to false -completed
// use if statement with expression if word is all lowercased, return true
// return the result

function isLowerCased(word) {
  var result = false;
  if (word === word.toLowerCase()) {
    return true;
  }
  return result;
}

// define a function named isLowerCased
//   that takes in one parameter, word
// set false as a value assigned to a result variable
// use an if statement to with a condition that checks if the word is completely lower cased
//   if the condition is passed as true, return true
// return the result variable
