/* exported getValue */
// define a function named getValue that
//   takes 2 parameters, an object and key -completed
// Use bracket notation with the parameters to find the value
//   return value of object at the key -completed

function getValue(object, key) {
  var result = object[key];
  return result;
}

// define a function named getValue that
//   takes 2 parameters, an object and key
// Use bracket notation with the parameters to find the value
//   assign that to a result variable
//     return the result variable
